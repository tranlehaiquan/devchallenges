import React from 'react';

interface Props {
  className?: string;
};

const Edit: React.FC<Props> = (props) => {
  return (
    <div>
      Edit
    </div>
  );
}

export default Edit;